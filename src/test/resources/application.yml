spring:
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop  # Allows Hibernate to manage schema in tests
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

#spring:
#  test:
#    database:
#      replace: none
#  jpa:
#    hibernate:
#      ddl-auto: create-drop
#    database-platform: org.hibernate.dialect.H2Dialect
#    show-sql: true
